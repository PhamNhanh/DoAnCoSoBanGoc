@model ViewModel

@{
    ViewData["Title"] = "Trang chủ";
}

<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>JobPlus</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="manifest" href="~/site.webmanifest">
    <link rel="shortcut icon" type="image/x-icon" href="~/assets/img/favicon.ico">
    <!-- Vendor CSS Files -->
    <link href="~/assets/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/assets/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
    <link href="~/assets/vendor/aos/aos.css" rel="stylesheet">
    <link href="~/assets/vendor/glightbox/css/glightbox.min.css" rel="stylesheet">
    <link href="~/assets/vendor/swiper/swiper-bundle.min.css" rel="stylesheet">

    <!-- Main CSS File -->
    <link href="~/assets/css/main.css" rel="stylesheet">
</head>

<body>
    <div class="container-fluid p-0">
        <!-- Slider Section -->
        <div id="heroSlider" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="~/images/elements/g1.jpg" class="d-block w-100 img-fluid" alt="Banner">
                    <div class="carousel-caption d-flex flex-column justify-content-center align-items-center text-center">
                        <h1 class="text-uppercase fw-bold text-light" style="font-size: 5rem; text-shadow: 3px 3px 6px #000;">
                            Bạn có năng lực
                        </h1>
                        <h2 class="text-light fw-semibold mt-3" style="font-size: 3rem; text-shadow: 3px 3px 6px #000;">
                            Chúng tôi có công việc
                        </h2>
                    </div>
                </div>
            </div>
        </div>

        <!-- Chuyên ngành nổi bật -->
        <section class="our-services py-5">
            <div class="container">
                <div class="text-center mb-5">
                    <span class="text-uppercase text-secondary">QUYẾT ĐỊNH TƯƠNG LAI CỦA BẠN</span>
                    <h2 class="fw-bold">Chuyên ngành nổi bật</h2>
                </div>
                <div class="row g-4">
                    @foreach (var major in Model.Majors)
                    {
                        <div class="col-xl-3 col-lg-4 col-md-6">
                            <div class="card text-center shadow-sm">
                                <div class="card-body">
                                    <h5 class="card-title"><a href="#" class="text-decoration-none">@major.ChuyenNganhName</a></h5>
                                    <p class="text-muted">(@major.JobCount)</p>
                                </div>
                            </div>
                        </div>
                    }
                </div>
                <div class="text-center mt-4">
                    <a href="@Url.Action("ListBaiTuyenDung", "NTD")" class="btn btn-outline-primary">Tất cả bài tuyển dụng</a>
                </div>
            </div>
        </section>

        <!-- Danh sách ứng viên -->
        <section class="job-listings py-5">
            <div class="container">
                <h2 class="fw-bold text-center mb-5">Các ứng viên phù hợp</h2>
                <div class="row g-4">
                    @foreach (var ungvien in Model.ApplicationUsers)
                    {
                        if (!string.IsNullOrEmpty(ungvien.FullName))
                        {
                            <div class="col-md-4">
                                <div class="card shadow-sm">
                                    <a href="@Url.Action("IndexProfileUV", "NTD", new { id = ungvien.Id })">
                                        <img src="@ungvien.image_url" class="card-img-top" alt="@ungvien.FullName" style="height: 200px; object-fit: cover;">
                                    </a>
                                    <div class="card-body">
                                        <h5 class="card-title text-center" style="font-size: 1.5rem; font-style: italic;">
                                            <a href="@Url.Action("IndexProfileUV", "NTD", new { id = ungvien.Id })">@ungvien.FullName</a>
                                        </h5>
                                        <p class="text-center">@ungvien.SDT_UngVien</p>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                </div>
            </div>
        </section>


    </div>
</body>
