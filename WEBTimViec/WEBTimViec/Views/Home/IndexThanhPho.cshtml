@model IEnumerable<WEBTimViec.Models.ThanhPho>

@{
    ViewData["Title"] = "IndexThanhPho";
}

<h1>IndexThanhPho</h1>

<p>
    <a asp-action="AddThanhPho">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThanhPho_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ThanhPho_name)
            </td>
@*             <td>
                <a asp-action="Edit" asp-route-id="@item.ThanhPho_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ThanhPho_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ThanhPho_id">Delete</a>
            </td> *@
        </tr>
}
    </tbody>
</table>
